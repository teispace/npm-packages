/**
 * Placeholder plugin for the Embed extension.
 * The EmbedNode is registered via getNodes() in the extension.
 * Commands are registered via onRegister().
 * This plugin exists for future embed-specific behaviors (e.g., auto-unfurl).
 */
export function EmbedCommandPlugin(): null {
  return null;
}
